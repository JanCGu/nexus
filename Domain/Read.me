The domain project only contains the entities and aggregates as they are the direct representation of the model on the business side. Therefore this project only contains and should only contain pure C#, as any other feature like Entity to Json would not be a part of the business logic. This functionality still exists, but is present in the Core project.