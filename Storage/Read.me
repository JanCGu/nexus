The idear behind this DLL is that it plugs into the main application.
This allows to easily replace the method or the source of how and where the chipcard-information-objects are retrieved.

Furthermore it allows the storage of the chipcards. This allows an easy access from a clients point of view, because it caches the independence unreliablity of the data provider.